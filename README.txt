#Execution Step
1. CodingTestPack--> TestPageTest.java --> RightClick on the class --> Run As --> TESTNG test

#Download chromedriver.exe compatible with the OS and place it inside ChromeDriver Folder rename it as chromedriver.exe

#Bugs
Observed few bugs in application
1. In Sign In Page the login button is enabled even when password field is empty
2. In Sign Up Button Vorname and nachname field accepts special char as well less than 3 char